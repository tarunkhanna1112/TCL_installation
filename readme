THIS REPOSITORY CONTAINS ALL THE TCL EXTENSION/LIBRARIES NEEDED TO BE INSTALLED TO ANY OF THE SHARED CODES

INSTALLATION DIRECTION

- Install the standard tcl shell (tclsh 8.5 is the version the code is tested against) (should be available from the standard linux repositories like yum)

-Install tcl math library for mathematical operations in the code
	-Untar Tcllib-1.16.tar.gz
	-In the untar directory just run ‘tclsh installer.tcl’ to install the tcl libraries.

